#include "main.h"
/**
 * _isupper - check upper case
 *
 * @c: alphabet input
 *
 * return : 1 (sucsses) , else 0 (not)
 */

int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
return (0);
}
