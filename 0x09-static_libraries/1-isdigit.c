#include "main.h"
/**
 * _isupper - check digi arring
 *
 * @c: no. input
 *
 * return : 1 (sucsses) , else 0 (not)
 */


int _isupper(int c)
{
if (c >= 48 && c <= 59)
{
return (1);
}
else
return (0);
}