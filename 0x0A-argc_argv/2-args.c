#include <stdio. h>
/**
* main - print no. o argumen pass n2 program
* @argc: nt
* @argv: list
* Return: 0
*/
int main(int argc, char const *argv[])
{
	int il = 0;

while (argc--)
{
printf("%s\n", argu[il]);
il = il + 1;
}
return (0);
}
